name: Octo Organization CI

on:
  push:
    branches: main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: Run a one-line script
        run: echo Hello from Octo Organization
      - name: script test
        run: ./scripts/test.sh
      - uses: actions/checkout@master
        with:
          name: xwskx/github-action-test
          path: checkout-test
      - name: list files
        run: ls -la
